#pragma once
#include <iostream>
#include <string>
#include <vector>
#include <stdio.h>
#include <string.h>

using namespace std;

namespace Utilities
{
	vector<const char*> FindCommonCStrings( vector<const char*> wantedList, vector<const char*> avalableList);
};

